apiVersion: batch/v1
kind: CronJob
metadata:
  name: trace-auditor
  namespace: default
spec:
  schedule: "*/15 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
          - name: auditor
            image: ghcr.io/insidemedia/trace-auditor:0.1.0 # Update registry if needed
            imagePullPolicy: Always
            envFrom:
            - secretRef:
                name: trace-auditor-secrets
            resources:
              requests:
                cpu: 100m
                memory: 128Mi
              limits:
                cpu: 250m
                memory: 256Mi
